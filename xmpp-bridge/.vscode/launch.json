{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run xmpp-bridge",
            "type": "debugpy",
            "request": "launch",
            "module": "xmpp_bridge",
            "envFile": "${workspaceFolder}/.env",
            "justMyCode": true,
            "console": "integratedTerminal"
        }
    ]
}
